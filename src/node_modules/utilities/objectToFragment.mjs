import { always, compose, ifElse, isEmpty, join, map, not, toPairs } from 'ramda'

export default ifElse(
  compose(not, isEmpty),
  compose(
    value => `#${value}`,
    join('&'),
    map(join('=')),
    map(map(encodeURIComponent)),
    toPairs
  ),
  always('')
)