import { compose, fromPairs, isNil, map, reject, replace, split } from 'ramda'

export default compose(
  map(decodeURIComponent),
  reject(isNil),
  fromPairs,
  map(split('=')),
  split('&'),
  replace(/^\?|\#/, '')
)